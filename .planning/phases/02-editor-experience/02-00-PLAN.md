---
phase: 02-editor-experience
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - MarkdownEditorTests/MarkdownHighlighterTests.swift
  - MarkdownEditorTests/UnsavedChangesTests.swift
  - MarkdownEditor.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - EDIT-03
  - EDIT-04

must_haves:
  truths:
    - "Test target MarkdownEditorTests builds without errors"
    - "MarkdownHighlighterTests.swift contains failing stubs for EDIT-04 (5 patterns: heading, bold, italic, code, link)"
    - "UnsavedChangesTests.swift contains failing stubs for EDIT-03 (indicator appears on change, disappears on save)"
  artifacts:
    - path: "MarkdownEditorTests/MarkdownHighlighterTests.swift"
      provides: "XCTest stubs for syntax highlighting"
      contains: "testHeadingColoring, testBoldColoring, testItalicColoring, testCodeColoring, testLinkColoring"
    - path: "MarkdownEditorTests/UnsavedChangesTests.swift"
      provides: "XCTest stubs for unsaved changes indicator"
      contains: "testTitleShowsAsteriskWhenUnsaved, testTitleClearsAsteriskAfterSave"
  key_links:
    - from: "MarkdownEditorTests/MarkdownHighlighterTests.swift"
      to: "MarkdownEditor/HighlightingService.swift"
      via: "@testable import MarkdownEditor"
      pattern: "@testable import MarkdownEditor"
---

<objective>
Create the XCTest test target and failing test stubs that Wave 1 and Wave 2 must satisfy.

Purpose: Nyquist compliance — every automated behavior must have a test before implementation begins. These stubs fail by design (tested modules don't exist yet) and become green after Wave 1-2 execution.
Output: MarkdownEditorTests/ directory with two test files + test target registered in Xcode project.
</objective>

<execution_context>
@/Users/davidlenir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidlenir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-editor-experience/02-RESEARCH.md
@.planning/phases/02-editor-experience/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MarkdownEditorTests test target to Xcode project</name>
  <files>MarkdownEditorTests/MarkdownHighlighterTests.swift, MarkdownEditorTests/UnsavedChangesTests.swift, MarkdownEditor.xcodeproj/project.pbxproj</files>

  <read_first>
    - MarkdownEditor.xcodeproj/project.pbxproj (see current target structure before editing)
    - MarkdownEditor/MarkdownDocument.swift (confirm module name "MarkdownEditor" for @testable import)
  </read_first>

  <action>
Create the MarkdownEditorTests directory and two test stub files. Then register both files and a new XCTest target in project.pbxproj.

**Step 1 — Create MarkdownHighlighterTests.swift:**

```swift
// MarkdownEditorTests/MarkdownHighlighterTests.swift
import XCTest
@testable import MarkdownEditor

final class MarkdownHighlighterTests: XCTestCase {

    // EDIT-04: heading line starting with # gets green foreground color
    func testHeadingColoring() throws {
        let result = HighlightingService.applyMarkdownColors(to: "# Hello")
        let range = result.startIndex ..< result.endIndex
        let colors = result[range].map { $0.1.foregroundColor }
        XCTAssertTrue(colors.contains(.init(uiColor: UIColor { tc in
            tc.userInterfaceStyle == .dark
                ? UIColor(red: 0.3, green: 0.8, blue: 0.3, alpha: 1)
                : UIColor(red: 0, green: 0.6, blue: 0, alpha: 1)
        })), "Heading text should have markdownHeading color")
        // Stub: will fail until HighlightingService exists — EXPECTED RED
        XCTFail("Stub — implement HighlightingService in Wave 1")
    }

    // EDIT-04: **bold** text gets blue foreground + bold font
    func testBoldColoring() throws {
        XCTFail("Stub — implement HighlightingService in Wave 1")
    }

    // EDIT-04: *italic* text gets purple foreground + italic font
    func testItalicColoring() throws {
        XCTFail("Stub — implement HighlightingService in Wave 1")
    }

    // EDIT-04: `code` text gets orange foreground + monospaced font
    func testCodeColoring() throws {
        XCTFail("Stub — implement HighlightingService in Wave 1")
    }

    // EDIT-04: [link](url) text gets cyan foreground + underline
    func testLinkColoring() throws {
        XCTFail("Stub — implement HighlightingService in Wave 1")
    }
}
```

**Step 2 — Create UnsavedChangesTests.swift:**

```swift
// MarkdownEditorTests/UnsavedChangesTests.swift
import XCTest
@testable import MarkdownEditor

final class UnsavedChangesTests: XCTestCase {

    // EDIT-03: buildTitle() appends " *" when document.hasUnsavedChanges == true
    func testTitleShowsAsteriskWhenUnsaved() throws {
        // Stub: EditorView.buildTitle() doesn't exist yet — EXPECTED RED
        XCTFail("Stub — implement EditorView.buildTitle() in Wave 2")
    }

    // EDIT-03: buildTitle() returns plain filename when document.hasUnsavedChanges == false
    func testTitleClearsAsteriskAfterSave() throws {
        XCTFail("Stub — implement EditorView.buildTitle() in Wave 2")
    }
}
```

**Step 3 — Register test target in project.pbxproj:**

Add a new XCTest unit test target named "MarkdownEditorTests" to project.pbxproj. Follow these exact steps:

1. Open MarkdownEditor.xcodeproj in Xcode (use `open MarkdownEditor.xcodeproj` from project root)
2. In Xcode: File > New > Target > Unit Testing Bundle
   - Product Name: `MarkdownEditorTests`
   - Team: (none — personal project)
   - Bundle Identifier: `com.markdowneditor.MarkdownEditorTests`
   - Language: Swift
   - Project: MarkdownEditor
   - Target to be Tested: MarkdownEditor
3. Click Finish. Xcode adds the target to project.pbxproj automatically.
4. Delete the auto-generated `MarkdownEditorTests.swift` Xcode creates (replace with the two files from Steps 1-2).
5. Add `MarkdownHighlighterTests.swift` and `UnsavedChangesTests.swift` to the MarkdownEditorTests group in Xcode (drag from Finder into Xcode project navigator under MarkdownEditorTests/).

**Alternative (CLI-only):** If Xcode cannot be opened interactively, use `xcodegen` or manually edit project.pbxproj by adding the required PBXNativeTarget, PBXGroup, PBXBuildFile, and PBXFileReference entries following the pattern of the existing MarkdownEditor target. The Xcode interactive approach is preferred.

**Critical:** The test target must have `@testable import MarkdownEditor` compile-able. Ensure the MarkdownEditor target has "Enable Testability" set to YES in its Build Settings (Xcode sets this automatically for debug builds when a test target is added via the GUI).
  </action>

  <verify>
    <automated>cd /Users/davidlenir/dev/iOS-markdown-editor && xcodebuild test -scheme MarkdownEditor -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "(FAILED|error:|MarkdownHighlighterTests|UnsavedChangesTests|Test session)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `MarkdownEditorTests/MarkdownHighlighterTests.swift` exists with exactly these 5 test methods: `testHeadingColoring`, `testBoldColoring`, `testItalicColoring`, `testCodeColoring`, `testLinkColoring`
    - `MarkdownEditorTests/UnsavedChangesTests.swift` exists with exactly these 2 test methods: `testTitleShowsAsteriskWhenUnsaved`, `testTitleClearsAsteriskAfterSave`
    - Both files contain `@testable import MarkdownEditor`
    - Both files contain `XCTFail("Stub` — confirms they are intentionally failing stubs
    - `xcodebuild test` compiles the test target without build errors (tests may fail with XCTFail, that is correct)
    - `grep -r "MarkdownEditorTests" MarkdownEditor.xcodeproj/project.pbxproj` returns at least 3 lines (target registered)
  </acceptance_criteria>

  <done>Test target builds. Both stub files exist with failing XCTFail stubs. Wave 1 and Wave 2 plans replace the stubs with real assertions.</done>
</task>

</tasks>

<verification>
Run full test build to confirm target compiles:
`cd /Users/davidlenir/dev/iOS-markdown-editor && xcodebuild build-for-testing -scheme MarkdownEditor -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5`

Expected result: `BUILD SUCCEEDED` (test stubs will fail at runtime with XCTFail, not at compile time).
</verification>

<success_criteria>
- MarkdownEditorTests target registered in project.pbxproj
- MarkdownHighlighterTests.swift compiles with @testable import MarkdownEditor
- UnsavedChangesTests.swift compiles with @testable import MarkdownEditor
- `xcodebuild build-for-testing` returns BUILD SUCCEEDED
- Test stubs fail with expected XCTFail messages (not compiler errors)
</success_criteria>

<output>
After completion, create `.planning/phases/02-editor-experience/02-00-SUMMARY.md`
</output>
