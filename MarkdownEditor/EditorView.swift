import SwiftUI

/// Placeholder stub — full implementation in Plan 03.
struct EditorView: View {
    @EnvironmentObject private var appState: AppState

    var body: some View {
        Text("Editor — coming in Plan 03")
            .foregroundStyle(.secondary)
    }
}
